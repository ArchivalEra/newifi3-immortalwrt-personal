# (C) H3C 模板 — 主模块

学校认证网关确认为 **H3C（华三）**。此模块按 H3C Web 认证协议实现。

- 老式 H3C 门户常带挑战码/JS 编码，密码字段按页面实际机制提交
- 门户若支持 HTTPS，登录走 HTTPS（TLS ECDHE = 即时一次性密钥，自签证书 `--insecure`）
- 探测掉线：连通性探针 URL 被重定向到门户 = 掉线 → 触发重登

状态：骨架占位。刷机后抓认证页（URL/挑战机制/证书）→ 按 H3C 协议填充实现。
